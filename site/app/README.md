# Public routes

The deployable public Next.js routes currently live in the repository root `app/`
folder. They are documented here as the intended `/site/app` boundary without
moving files that current deployment still expects at root.
