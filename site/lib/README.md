# Public libraries

The deployable public data adapters and content helpers currently live in the
repository root `lib/` folder. This marker records the intended `/site/lib`
boundary while root deployment is preserved.
