# Public components

The deployable public UI components currently live in the repository root
`components/` folder. This marker records the intended `/site/components`
boundary while root deployment is preserved.
